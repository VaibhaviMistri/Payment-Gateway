#Backend Setup
cd server
npm install

Frontend Setup
cd cliet
npm install

# Start the Express backend
cd server
node --watch index.js

# Start the React frontend (in a new terminal)
cd client
npm start

