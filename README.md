# Backend Setup
cd server
npm install

# Frontend Setup
cd clinet
npm install

# Start the express backend
cd server
node --watch index.js

# Start the React frontend (in a new terminal)
cd client
npm start

